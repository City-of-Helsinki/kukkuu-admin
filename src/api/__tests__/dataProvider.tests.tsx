import { TestContext } from 'react-admin';
import { mount } from 'enzyme';


describe('dataProvider', () => {
    test('return defined result when handler exist for certain Resource', () => {
        
    })
})